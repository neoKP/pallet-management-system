# 📊 คู่มืออธิบาย Analytics Dashboard

## ระบบจัดการพาเลทหมุนเวียน (Pallet Management System)

---

## 🎯 ภาพรวม

Analytics Dashboard เป็นหน้าจอแสดงผลข้อมูลเชิงวิเคราะห์ เพื่อช่วยให้ผู้ใช้งานเข้าใจ **การเคลื่อนไหวของพาเลท** ภายในองค์กร ติดตามประสิทธิภาพการหมุนเวียน และวางแผนการจัดการพาเลทในอนาคต

---

## 📈 1. KPI Cards (การ์ดตัวชี้วัดหลัก)

### 1.1 การเคลื่อนไหวรวม

| หัวข้อ | รายละเอียด |
|--------|-----------|
| **ข้อมูลที่แสดง** | จำนวนรายการเคลื่อนไหวพาเลททั้งหมด (IN + OUT + MAINTENANCE) |
| **แหล่งข้อมูล** | นับจากตาราง `transactions` ตามช่วงเวลาและตัวกรองที่เลือก |
| **วิธีดู** | ตัวเลขใหญ่ = จำนวนรายการ, ลูกศรขึ้น/ลง = แนวโน้มเทียบช่วงก่อน |
| **ประโยชน์ปัจจุบัน** | เห็นปริมาณงานรวมของทีม ตรวจสอบว่าวันนี้มีการเคลื่อนไหวมากน้อยแค่ไหน |
| **ประโยชน์อนาคต** | วางแผนกำลังคน, ประเมิน Workload ของแต่ละช่วงเวลา |

### 1.2 พาเลทในคลัง

| หัวข้อ | รายละเอียด |
|--------|-----------|
| **ข้อมูลที่แสดง** | จำนวนพาเลททั้งหมดที่อยู่ในคลังของทุกสาขา |
| **แหล่งข้อมูล** | รวมจาก `stock` ของทุกสาขาและทุกประเภทพาเลท |
| **วิธีดู** | ตัวเลข + หน่วย "ชิ้น" |
| **ประโยชน์ปัจจุบัน** | ทราบจำนวนพาเลทพร้อมใช้งานทั้งหมด |
| **ประโยชน์อนาคต** | วางแผนการจัดซื้อพาเลทใหม่, ประเมินความเพียงพอ |

### 1.3 กำลังโอนย้าย

| หัวข้อ | รายละเอียด |
|--------|-----------|
| **ข้อมูลที่แสดง** | จำนวนพาเลทที่กำลังอยู่ระหว่างการโอนย้ายระหว่างสาขา |
| **แหล่งข้อมูล** | นับจาก transactions ที่มี status = "PENDING" หรือ "IN_TRANSIT" |
| **วิธีดู** | ตัวเลข = พาเลทที่ยังไม่ถึงปลายทาง |
| **ประโยชน์ปัจจุบัน** | ติดตามพาเลทที่กำลังเดินทาง ป้องกันการสูญหาย |
| **ประโยชน์อนาคต** | วิเคราะห์เวลาขนส่งเฉลี่ย ปรับปรุงกระบวนการโลจิสติกส์ |

### 1.4 อัตราหมุนเวียน

| หัวข้อ | รายละเอียด |
|--------|-----------|
| **ข้อมูลที่แสดง** | เปอร์เซ็นต์ความถี่ในการใช้งานพาเลท |
| **แหล่งข้อมูล** | คำนวณจาก (จำนวนพาเลทที่ถูกใช้งาน / จำนวนพาเลททั้งหมด) × 100 |
| **วิธีดู** | ยิ่งสูงยิ่งดี = พาเลทถูกใช้งานคุ้มค่า |
| **ประโยชน์ปัจจุบัน** | ประเมินว่าพาเลทถูกใช้งานเต็มประสิทธิภาพหรือไม่ |
| **ประโยชน์อนาคต** | ปรับปรุงการกระจายพาเลท, ลดการค้างสต็อก |

### 1.5 เข้าซ่อมบำรุง

| หัวข้อ | รายละเอียด |
|--------|-----------|
| **ข้อมูลที่แสดง** | เปอร์เซ็นต์พาเลทที่เข้าซ่อมบำรุง |
| **แหล่งข้อมูล** | คำนวณจาก (จำนวน MAINTENANCE / จำนวนรายการทั้งหมด) × 100 |
| **วิธีดู** | ยิ่งต่ำยิ่งดี = พาเลทสภาพดี, สีส้ม = เตือน |
| **ประโยชน์ปัจจุบัน** | เฝ้าระวังสภาพพาเลท ป้องกันปัญหา |
| **ประโยชน์อนาคต** | วางแผนงบประมาณซ่อมบำรุง, จัดซื้อพาเลททดแทน |

---

## 📊 2. กราฟเส้น: การเคลื่อนไหวพาเลท 7 วันล่าสุด

| หัวข้อ | รายละเอียด |
|--------|-----------|
| **ข้อมูลที่แสดง** | จำนวนรายการเคลื่อนไหวพาเลทในแต่ละวัน ย้อนหลัง 7 วัน |
| **แหล่งข้อมูล** | กรองจาก `transactions` 7 วันล่าสุด แยกตามวันที่ |
| **วิธีดู** | แกน X = วันที่, แกน Y = จำนวนรายการ, เส้นขึ้น = ปริมาณงานเพิ่ม |
| **ประโยชน์ปัจจุบัน** | เห็นแนวโน้มสั้นๆ ว่าปริมาณงานเพิ่มหรือลด |
| **ประโยชน์อนาคต** | ระบุวันที่มีปริมาณงานสูง (เช่น ต้นสัปดาห์) เพื่อจัดกำลังคน |

---

## 🥧 3. กราฟวงกลม: สัดส่วนประเภทการเคลื่อนไหว

| หัวข้อ | รายละเอียด |
|--------|-----------|
| **ข้อมูลที่แสดง** | สัดส่วนของ IN, OUT, MAINTENANCE ในช่วงเวลาที่เลือก |
| **แหล่งข้อมูล** | นับจำนวนแต่ละประเภทจาก `transactions` |
| **วิธีดู** | ส่วนใหญ่ = ประเภทที่เกิดขึ้นบ่อย, คลิกเพื่อดูรายละเอียด |
| **ประโยชน์ปัจจุบัน** | เข้าใจว่าการทำงานส่วนใหญ่เป็นรับเข้าหรือจ่ายออก |
| **ประโยชน์อนาคต** | ปรับปรุงกระบวนการทำงานตามสัดส่วน |

---

## 📊 4. กราฟแท่ง: พาเลทในคลังแยกตามสาขา

| หัวข้อ | รายละเอียด |
|--------|-----------|
| **ข้อมูลที่แสดง** | จำนวนพาเลทในคลังของแต่ละสาขา เรียงจากมากไปน้อย |
| **แหล่งข้อมูล** | รวม stock แยกตาม branchId |
| **วิธีดู** | แท่งยาว = สาขามีพาเลทเยอะ, คลิกเพื่อ Drill Down ดูรายละเอียด |
| **ประโยชน์ปัจจุบัน** | ระบุสาขาที่มีพาเลทมากเกินหรือน้อยเกินไป |
| **ประโยชน์อนาคต** | วางแผนโอนย้ายพาเลทระหว่างสาขาให้สมดุล |

---

## 🎨 5. กราฟแท่ง: สัดส่วนพาเลทแยกตามประเภท

| หัวข้อ | รายละเอียด |
|--------|-----------|
| **ข้อมูลที่แสดง** | จำนวนพาเลทแยกตามประเภท (เช่น EUR, CHEP, Wooden) |
| **แหล่งข้อมูล** | รวม stock แยกตาม palletTypeId |
| **วิธีดู** | สีต่างกันตามประเภท, แท่งยาว = ใช้งานเยอะ |
| **ประโยชน์ปัจจุบัน** | ทราบว่าพาเลทประเภทไหนมีใช้มากที่สุด |
| **ประโยชน์อนาคต** | วางแผนจัดซื้อพาเลทแต่ละประเภทตามความต้องการ |

---

## 🔄 6. Sankey Diagram: การไหลของพาเลทระหว่างสาขา

| หัวข้อ | รายละเอียด |
|--------|-----------|
| **ข้อมูลที่แสดง** | แสดงการโอนย้ายพาเลทจากสาขาหนึ่งไปอีกสาขา |
| **แหล่งข้อมูล** | วิเคราะห์คู่ fromBranch → toBranch จาก transactions |
| **วิธีดู** | เส้นหนา = โอนย้ายบ่อย/เยอะ, เส้นบาง = น้อย |
| **ประโยชน์ปัจจุบัน** | เห็นความสัมพันธ์ระหว่างสาขา ใครส่งให้ใครบ่อย |
| **ประโยชน์อนาคต** | วางแผนเส้นทางโลจิสติกส์ ลดต้นทุนขนส่ง |

---

## 🎛️ 7. Gauge Charts (มาตรวัด)

### 7.1 ประสิทธิภาพการหมุนเวียน

| หัวข้อ | รายละเอียด |
|--------|-----------|
| **ข้อมูลที่แสดง** | อัตราการใช้งานพาเลท (%) |
| **วิธีดู** | เข็มชี้ที่สีเขียว = ดี, สีเหลือง = ปานกลาง, สีแดง = ต้องปรับปรุง |
| **ประโยชน์** | ดูภาพรวมสุขภาพของระบบได้ทันที |

### 7.2 ความจุคลังพาเลท

| หัวข้อ | รายละเอียด |
|--------|-----------|
| **ข้อมูลที่แสดง** | จำนวนพาเลทปัจจุบันเทียบกับความจุสูงสุด |
| **วิธีดู** | เข็มใกล้ขีด max = คลังใกล้เต็ม |
| **ประโยชน์** | วางแผนขยายพื้นที่หรือกระจายพาเลท |

---

## 📈 8. Comparison Card: การเคลื่อนไหวประจำเดือน

| หัวข้อ | รายละเอียด |
|--------|-----------|
| **ข้อมูลที่แสดง** | เปรียบเทียบจำนวนรายการเดือนนี้กับเดือนก่อน |
| **แหล่งข้อมูล** | นับ transactions แยกตามเดือน |
| **วิธีดู** | สีเขียว = ดีขึ้น, สีแดง = ลดลง, % = ส่วนต่าง |
| **ประโยชน์ปัจจุบัน** | ติดตามผลการทำงานรายเดือน |
| **ประโยชน์อนาคต** | ตั้งเป้าหมายและวัดผล KPI |

---

## 🔮 9. Forecast Chart: พยากรณ์การหมุนเวียนพาเลท (AI)

| หัวข้อ | รายละเอียด |
|--------|-----------|
| **ข้อมูลที่แสดง** | คาดการณ์จำนวนรายการใน 7 วันข้างหน้า |
| **แหล่งข้อมูล** | ใช้ข้อมูลย้อนหลังมาคำนวณแนวโน้ม (Simple Moving Average) |
| **วิธีดู** | เส้นทึบ = ข้อมูลจริง, เส้นประ = คาดการณ์ |
| **ประโยชน์ปัจจุบัน** | เตรียมตัวล่วงหน้าสำหรับงานที่จะเข้ามา |
| **ประโยชน์อนาคต** | วางแผนกำลังคนและทรัพยากรล่วงหน้า |

---

## 📊 10. Year-over-Year: เปรียบเทียบการหมุนเวียนรายปี

| หัวข้อ | รายละเอียด |
|--------|-----------|
| **ข้อมูลที่แสดง** | เปรียบเทียบจำนวนรายการระหว่างปี (เช่น 2025 vs 2026) |
| **แหล่งข้อมูล** | รวม transactions แยกตามปี |
| **วิธีดู** | แท่งคู่กัน เปรียบเทียบปีต่อปี |
| **ประโยชน์ปัจจุบัน** | เห็นการเติบโตหรือถดถอยของธุรกิจ |
| **ประโยชน์อนาคต** | วางแผนกลยุทธ์ระยะยาว ตั้งเป้าหมายรายปี |

---

## 📊 11. Week-over-Week: แนวโน้มการหมุนเวียน 8 สัปดาห์

| หัวข้อ | รายละเอียด |
|--------|-----------|
| **ข้อมูลที่แสดง** | จำนวนรายการในแต่ละสัปดาห์ ย้อนหลัง 8 สัปดาห์ |
| **แหล่งข้อมูล** | กลุ่ม transactions ตามสัปดาห์ |
| **วิธีดู** | แท่งสีม่วงแสดงแต่ละสัปดาห์, กดปุ่ม "View Report" ดูรายละเอียด |
| **ประโยชน์ปัจจุบัน** | เห็นแนวโน้มระยะกลาง ว่าปริมาณงานเพิ่มหรือลด |
| **ประโยชน์อนาคต** | ระบุ Pattern การทำงาน เช่น สัปดาห์ต้นเดือนมักมีงานเยอะ |

---

## 📊 12. Waterfall Chart: การเปลี่ยนแปลงสต็อกพาเลท

| หัวข้อ | รายละเอียด |
|--------|-----------|
| **ข้อมูลที่แสดง** | แสดงการเปลี่ยนแปลงของสต็อก: เริ่มต้น → +IN → -OUT → -MAINTENANCE → ปัจจุบัน |
| **แหล่งข้อมูล** | คำนวณจากยอดทั้งหมดของแต่ละประเภท transaction |
| **วิธีดู** | สีเขียว = เพิ่ม, สีแดง = ลด, กล่องสีน้ำเงิน = ยอดรวม |
| **ประโยชน์ปัจจุบัน** | เข้าใจที่มาที่ไปของสต็อก ว่าเพิ่ม/ลดจากอะไร |
| **ประโยชน์อนาคต** | วิเคราะห์สาเหตุที่ทำให้สต็อกเปลี่ยนแปลง |

---

## 🗓️ 13. Heatmap: ความหนาแน่นการหมุนเวียน (รายปี)

| หัวข้อ | รายละเอียด |
|--------|-----------|
| **ข้อมูลที่แสดง** | แสดงจำนวนการเคลื่อนไหวในแต่ละวันของปี (คล้าย GitHub Contribution Graph) |
| **แหล่งข้อมูล** | นับ transactions ในแต่ละวัน 84 วันย้อนหลัง |
| **วิธีดู** | สีเข้ม = กิจกรรมเยอะ, สีจาง = กิจกรรมน้อย, Hover ดูรายละเอียด |
| **ประโยชน์ปัจจุบัน** | เห็นรูปแบบการทำงานในมุมมองปฏิทิน |
| **ประโยชน์อนาคต** | ระบุวันที่มีกิจกรรมสูง (เช่น ศุกร์มักยุ่ง) เพื่อจัดกำลังคน |

---

## 🧠 14. AI Insights: คำแนะนำอัตโนมัติ

| หัวข้อ | รายละเอียด |
|--------|-----------|
| **ข้อมูลที่แสดง** | คำแนะนำที่สร้างจากการวิเคราะห์ข้อมูลอัตโนมัติ |
| **แหล่งข้อมูล** | ประเมินจาก KPIs, สถานะสาขา, อัตราซ่อมบำรุง |
| **ตัวอย่างคำแนะนำ** | |
| 🟡 สาขามีพาเลทในคลังสูง | แนะนำให้โอนย้ายไปสาขาที่ขาดแคลน |
| 🔴 พาเลทเข้าซ่อมบำรุงสูง | ตรวจสอบสาเหตุความเสียหาย |
| 🟢 การหมุนเวียนสมดุลดี | ระบบทำงานปกติ |
| **ประโยชน์** | ไม่ต้องวิเคราะห์เอง ระบบบอกปัญหาและแนวทางแก้ไขให้ |

---

## 🎚️ 15. Slicers (ตัวกรอง)

| ตัวกรอง | ผลลัพธ์ |
|---------|--------|
| **ปี** | กรองข้อมูลเฉพาะปีที่เลือก (เช่น 2025, 2026) |
| **ประเภทพาเลท** | กรองเฉพาะพาเลทประเภทที่ต้องการ |
| **สาขา** | กรองเฉพาะสาขาที่ต้องการดู |

---

## 💡 สรุปประโยชน์รวม

### สำหรับการทำงานประจำวัน

1. ✅ ติดตามปริมาณงานรายวัน
2. ✅ ตรวจสอบพาเลทที่กำลังโอนย้าย
3. ✅ ระบุสาขาที่มีพาเลทไม่สมดุล
4. ✅ เฝ้าระวังพาเลทที่ต้องซ่อมบำรุง

### สำหรับการวางแผนอนาคต

1. 📈 พยากรณ์ปริมาณงานล่วงหน้า
2. 📊 วิเคราะห์แนวโน้มรายสัปดาห์/รายปี
3. 🔄 ปรับปรุงการกระจายพาเลทระหว่างสาขา
4. 💰 วางแผนงบประมาณซ่อมบำรุงและจัดซื้อ

---

## 📞 ติดต่อสอบถามเพิ่มเติม

หากมีข้อสงสัยเกี่ยวกับการใช้งาน Analytics Dashboard สามารถติดต่อทีมพัฒนาระบบได้
